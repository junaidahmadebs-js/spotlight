// import { setCustomizeDataActiveTab } from "@/lib/features/(spotlight)/report/reportSlice";
// import { AppDispatch, RootState } from "@/lib/store";
// import React from "react";
// import { useDispatch, useSelector } from "react-redux";
 
// const useCustomizeData = () => {
//   const dispatch = useDispatch<AppDispatch>();
//   const { tabs } = useSelector((state: RootState) => state.spotlight.report);
//   const { excelData, dataTypes, activeTab } = tabs.customizeData;
 
//   const handleToChangeActiveTab = (tab: string) => {
//     // Dispatch action to change active tab if needed
//     dispatch(setCustomizeDataActiveTab(tab));
//   };
//   return { activeTab, excelData, dataTypes, handleToChangeActiveTab };
// };
 
// export default useCustomizeData;
 
 